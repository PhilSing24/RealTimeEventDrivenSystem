# RealTimeEventDrivenSystem

High-performance C++ feed handler that consumes Binance real-time market data (WebSocket) and publishes into a kdb+/KDB-X tick architecture (TP/RDB) via IPC.

## Documentation
See `docs/README.md` for architecture references, measurement notes, ADRs, and API notes.
